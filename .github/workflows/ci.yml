name: Deploy Dashboard

on:
  push:
    branches:
      - master

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Checkout Homer
        uses: actions/checkout@v3
        with:
          repository: "bastienwirtz/homer"

      - name: Build and Copy Config
        run: yarn install && yarn build && cp config.yml dist/assets/ && cp CNAME dist/

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
